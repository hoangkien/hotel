<!DOCTYPE html>
<html>
<head>
  <title>LearnBoostrap</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
  <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
  <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
  <![endif]-->
  <%= csrf_meta_tags %>
</head>
<body>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

  <%= render 'shared/header'%>
  <%= render 'shared/slide'%>
  <hr>
  <!-- <%= render 'shared/feature'%> -->
<!--     <header>
    1.1 Site name + description
    1.2 navigation
    1.3 feature content + slider
  </header> <!-- /header ends here-->

  <main class="container">
    <%= yield %>
  </main> <!-- /main ends here-->

  <footer>
    4.1 full screen background color for footer
    4.2 three columns with different panel content
    4.3 coppyright info
  </footer> <!-- /footer ends here-->
  <div class="col-md-1 col-xs-5 pull-right" id='cd-top'>
    <%= image_tag('back-to-top.png', width: '100%', class: 'cd-top')%>
  </div>
  <script type="text/javascript">
    $(window).scroll(function() {
      var height = $(window).scrollTop();
      console.log(height);
      if(height  > 750) {
        $("#cd-top").show(300);
      } else {
        $("#cd-top").hide(300);
      }
    });
    $(".cd-top").click(function(){
      $("html, body").animate({ scrollTop: 0 }, "slow");
      return false;
    });
  </script>
</body>
</html>
